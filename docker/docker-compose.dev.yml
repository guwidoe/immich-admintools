# Development docker-compose with local Redis for testing
# Use this when developing without a full Immich instance

services:
  redis:
    image: redis:7-alpine
    container_name: admin_tools_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:
